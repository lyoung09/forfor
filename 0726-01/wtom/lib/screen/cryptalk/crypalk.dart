import 'package:flutter/material.dart';

class CryptalkScreen extends StatelessWidget {
  const CryptalkScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
